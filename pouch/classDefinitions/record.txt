[settings]
class = CRecord
fields = \
    uint64 key|\
    string date|\
    string type|\
    uint64 grant_id|\
    address address|\
    string name|\
    string slug|\
    uint64 tx_cnt|\
    uint64 log_cnt|\
    uint64 donation_cnt|\
    uint64 core
includes = etherlib.h|balance.h
display_str = key, date, type, address, name, cnt, csv, json
